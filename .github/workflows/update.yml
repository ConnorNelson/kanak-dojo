name: Update Dojo

on: push

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: pwncollege/dojo-update@v1
        with:
          dojo: kanak~a5d0c869
          update_code: ${{ secrets.UPDATE_CODE }}
